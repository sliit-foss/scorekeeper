name: bootstrap
description: Bootstrap dependencies
runs:
  using: composite
  steps:
    - uses: actions/setup-node@v3
      with:
        node-version: 16.x
        cache: "pnpm"

    - name: Install dependencies
      run: pnpm install
      shell: bash